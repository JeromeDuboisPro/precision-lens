{
  "name": "precision-lens-web",
  "version": "1.0.0",
  "description": "Frontend tests for PrecisionLens dashboard",
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "testMatch": [
      "**/tests/**/*.test.js"
    ],
    "collectCoverageFrom": [
      "dashboard.js"
    ]
  }
}
